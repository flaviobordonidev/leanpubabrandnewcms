# Inizializzazione di Github


Risorse web

* https://help.github.com/articles/connecting-to-github-with-ssh/




## Apriamo il branch
non serve perché è rimasto aperto dal capitolo precedente




## Github sign_up sign_in

Per prima cosa creiamoci un account su www.github.com

![fig. a](01-beginning/07-github/03a-github_signup.png)

Se lo abbiamo già logghiamoci.

![fig. b](01-beginning/07-github/03b-github_signin.png)




## Github connessione con aws Cloud9

Per far comunicare Github con aws Cloud9 dobbiamo fargli condividere la chiave pubblica.

{title="terminale", lang=bash, line-numbers=off}
~~~~~~~~
$ cat ~/.ssh/id_rsa.pub
~~~~~~~~

Visualizziamo e copiamoci la chiave pubblica (public key) di aws Cloud9.

{title="terminale", lang=bash, line-numbers=off}
~~~~~~~~
$ cat ~/.ssh/id_rsa.pub
~~~~~~~~

users of the cloud IDE might be able to view their public key using the cat command, which can then be selected and copied. 

Once you’ve signed up for GitHub, you’ll need to add SSH keys to your account, which serve as a way to identify trusted computers without requiring passwords.1 To do this, follow the steps in the GitHub article “Generating SSH Keys” (Figure 2.2).






## Github nuovo repository


Una volta loggati creiamo un nuovo repository:

* nome        : rigenerabatterie1
* descrizione : sito web per ricarica batterie

![fig. c](01-beginning/07-github/03c-github_new_repository.png)

Appena creato il nuovo repository ci viene presentato un "Quick setup"

![fig. d](01-beginning/07-github/03d-github_quick_setup.png)

Aggiungiamo sul nostro git il repository remoto "rigenerabatterie1.git" creato sul nostro account github "flaviobordonidev" usando SSH.

{title="terminal", lang=bash, line-numbers=off}
~~~~~~~~
$ git remote add origin git@github.com:flaviobordonidev/rigenerabatterie1.git
~~~~~~~~

Se avessimo voluto usare HTTPS avremmo usato "git remote add origin https://github.com/flaviobordonidev/rigenerabatterie1.git"

Il comando "git remote" è per attivare il repository remoto su un server esterno (nel nostro caso github.com).
con "add origin" si dichiara che il nome di riferimento del repositroy remoto è "origin" (potevamo chiamarlo github ma per convenzione storica la stessa Github ha scelto di chiamarlo "origin").  

Prima di spostare il nostro git locale sul repository remoto Github dobbiamo chiudere il branch locale, tornare sul branch master ed effettuare il merge. 




## Chiudiamo il branch

se abbiamo finito le modifiche e va tutto bene:

{title="terminal", lang=bash, line-numbers=off}
~~~~~~~~
$ git checkout master
$ git merge gh
$ git branch -d gh
~~~~~~~~




## Facciamo un backup su Github

Dal nostro branch master di Git facciamo un backup di tutta l'applicazione sulla repository remota Github.

{title="terminal", lang=bash, line-numbers=off}
~~~~~~~~
$ git push origin master
~~~~~~~~

il comando "git push" sposta sul branch remoto "origin" il branch locale "master".

Spostiamo in remoto anche la parte dei tag in cui abbiamo messo la versione v.0.1.0

{title="terminal", lang=bash, line-numbers=off}
~~~~~~~~
$ git push origin master --tag
~~~~~~~~

![fig. e](01-beginning/07-github/03e-github_repository_overview.png)
