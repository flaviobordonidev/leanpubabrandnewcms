# posts show

Mostro il singolo articolo(post).

Nuova story:

A> Come visitatore del sito web                               
A> 
A> Voglio vedere l'articolo che scelgo dall'elenco
A> 
A> Così da poter leggere l'articolo

Quindi mi apro il branch

{title="terminal", lang=bash, line-numbers=off}
~~~~~~~~
$ git pull origin master
$ git checkout -b posts_index
$ git push origin posts_index
~~~~~~~~


...
...
...


## Aggiungo i campi autore e data


{title="terminal", lang=bash, line-numbers=off}
~~~~~~~~
$ rails g migration AddColumnsToPosts author_first_name:string author_last_name:string post_date:date
~~~~~~~~

[codice: posts db xx](#code-blogs-db-xx)

{title="db/migrate/xxx_add_columns_to_posts.rb", lang=ruby, line-numbers=on, starting-line-number=1}
~~~~~~~~
class AddColumnsToPosts < ActiveRecord::Migration
  def change
    add_column :posts, :author_first_name, :string
    add_column :posts, :author_last_name, :string
    add_column :posts, :post_date, :date
  end
end
~~~~~~~~

Effettuiamo il migrate del database

{title="terminal", lang=bash, line-numbers=off}
~~~~~~~~
$ rake db:migrate
~~~~~~~~

aggiungiamoli alla white-list nel controller

{title="controllers/posts_controller.rb", lang=ruby, line-numbers=on, starting-line-number=54}
~~~~~~~~
# Never trust parameters from the scary internet, only allow the white list through.
def post_params
  params.require(:post).permit(:image, :remove_image, :title, :content_short, :content, :content_first, :content_blockquote, :content_second, :author_first_name, :author_last_name, :post_date)
end
~~~~~~~~


