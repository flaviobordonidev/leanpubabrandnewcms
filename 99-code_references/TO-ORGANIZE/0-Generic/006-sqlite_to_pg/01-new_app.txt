# Nuova app Rails 5 utilizzando Cloud9 workspace




## Prepariamo un nuovo applicativo

{title="terminal", lang=bash, line-numbers=off}
~~~~~~~~
$ gem install rails -v 5.0.0.1

$ cd ~/workspace
$ rails _5.0.0.1_ new newapp

$ cd newapp
$ rails s -b $IP -p $PORT
~~~~~~~~

su cloud9 vai su "Share" e clicca sul link di "application" per aprire l'applicazione.

![fig. Share -> application: link](images/frontend_assets/02/share-application_link.png)
